import { Module } from '@nestjs/common';

// Módulo activities: notas, llamadas, correos, reuniones y tareas de seguimiento.
// Pendiente: controller, service y DTOs (class-validator).
@Module({})
export class ActivitiesModule {}
