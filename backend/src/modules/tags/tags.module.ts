import { Module } from '@nestjs/common';

// Módulo tags: etiquetas reutilizables para segmentar contactos y oportunidades.
// Pendiente: controller, service y DTOs (class-validator).
@Module({})
export class TagsModule {}
