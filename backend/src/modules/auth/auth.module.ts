import { Module } from '@nestjs/common';

// Módulo auth: registro, login y emisión de tokens JWT.
// Pendiente: controller, service, estrategia 'jwt' y DTOs (class-validator).
@Module({})
export class AuthModule {}
