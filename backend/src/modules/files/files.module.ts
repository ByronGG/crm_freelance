import { Module } from '@nestjs/common';

// Módulo files: archivos adjuntos asociados a otras entidades.
// Pendiente: controller, service y DTOs (class-validator).
@Module({})
export class FilesModule {}
