import { Module } from '@nestjs/common';

// Módulo contacts: personas y empresas con las que se hace negocio.
// Pendiente: controller, service y DTOs (class-validator).
@Module({})
export class ContactsModule {}
