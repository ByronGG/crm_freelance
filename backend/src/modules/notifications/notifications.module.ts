import { Module } from '@nestjs/common';

// Módulo notifications: avisos in-app (recordatorios, vencimientos, cambios de etapa).
// Pendiente: controller, service y DTOs (class-validator).
@Module({})
export class NotificationsModule {}
